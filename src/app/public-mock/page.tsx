import { PublicMockPage } from "@/components/Public_Mock_Page";

export const metadata = {
    title: "Public Mock - MockForge",
    description: "Explore and use public mock API projects created by the MockForge community"
}

export default function PublicMock() {
    return <PublicMockPage />;
}